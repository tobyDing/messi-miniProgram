<view 
  wx:if="{{inited}}"
  class="ms-transition {{classes}}" 
  style="-webkit-transition-duration:{{currentDuration}}ms; transition-duration:{{currentDuration}}ms; {{ display ? '' : 'display: none;' }}"
  bind:transitionend="onTransitionEnd"
>
  <slot></slot>
</view>